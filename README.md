# Ariabl Client
The client for Ariabl, a full stack application for developers to efficiently make their frontend code more accessible

# Contributors
Mason DeWitt (mason.dewitt@snhu.edu)

# Running the App

## In Development
- First run `npm install` to install all necessary depencies
- Next, run `npm run dev` to start the server
- The server will be locally hosted on port 5173 by default